import { ITask } from '@/types/ITask'

// export function getTaskById (state :any, id: number) {
//   return state.tasks.find((task: ITask) => task.id === id)
// }
// export function getLastId (state :any) {
//   return state.tasks[state.tasks.length - 1].id
// }
